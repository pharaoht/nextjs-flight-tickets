export type CabinSelectionType = {
    name: string;
    value: string;
}

export type CurrenciesType = {
    value: string;
    label: string;
}

export type AirportLocationType = {
    name: string;
    id: string;
}